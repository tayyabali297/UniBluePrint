## github clone 
copy the uniblue print url 
in vscode open the folder 
in the terminmal
git init should set it up as an existing git repo
git status to check if you have any commits to add probably not 
git commit -m "message here etc first commit"
then 
git push 